using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VotingWindow : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        gameObject.SetActive(false);
//        CanvasRenderer render = gameObject.GetComponent<CanvasRenderer>();

//        render.SetAlpha(0);
    }

    // Update is called once per frame
    void Update()
    {
        print("a");
    }
}
